
<nav class="navbar border-bottom navbar-expand-lg p-2 navbar-dark bg-dark">
    <div class="container">
      <a class="navbar-brand" href="#"> <img src="<%= data.brandIconUrl%>" alt="logo" width="50" height="40" style="border-radius: 50%; margin:0 6px; padding: 0 6px;"><%= data.brandName %></a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse " id="navbarNavAltMarkup">

        <% var links = data.links
         for (var key in links){ %> 
        
        <div class="navbar-nav ms-auto p-2">
          <a class="nav-link active m-0" aria-current="page" href="<%= links[key].url %>"><%= links[key].label%></a> 
        </div>
        <% } %>
      </div>
    </div>
  </nav>

